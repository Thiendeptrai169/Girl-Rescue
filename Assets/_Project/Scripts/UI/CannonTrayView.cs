using UnityEngine;

namespace DragonRescue.UI
{
    /// <summary>
    /// Placeholder for tray-level UI logic (like scrolling or expanding).
    /// Currently, LevelManager handles spawning cards directly into the tray parent.
    /// Keeping this script for future expansion (e.g. animating cards entering).
    /// </summary>
    public class CannonTrayView : MonoBehaviour
    {
        // Reserved for future Polish phase
    }
}
